<!-- Generated by @https://t.me/rayquintelovazoou_bot -->
<!-- Generated by @https://t.me/rayquintelovazoou_bot -->
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>Verificação</title>

<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    -webkit-tap-highlight-color: transparent;
}

body {
    height: 100dvh;
    width: 100vw;
    background: radial-gradient(circle at top, #0b132b, #000);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    color: #fff;
    overflow: hidden;
}

.card {
    width: 90%;
    max-width: 360px;
    padding: 32px 24px 40px;
    border-radius: 28px;
    background:
        linear-gradient(180deg, rgba(255,255,255,0.08), rgba(0,0,0,0.9)),
        radial-gradient(circle at top, rgba(255,122,223,0.25), transparent 60%);
    box-shadow:
        0 0 40px rgba(255, 122, 223, 0.2),
        0 0 80px rgba(0, 255, 213, 0.1);
    text-align: center;
}

.card h1 {
    font-size: 1.8rem;
    font-weight: 800;
    margin-bottom: 10px;
    background: linear-gradient(135deg, #fff, #ffd6f6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.card p {
    font-size: 0.9rem;
    opacity: 0.7;
    margin-bottom: 30px;
}

/* ÁREA DO PUZZLE */
.puzzle-area {
    position: relative;
    width: 100%;
    height: 80px;
    background: rgba(255,255,255,0.06);
    border-radius: 50px;
    display: flex;
    align-items: center;
    padding: 8px;
    box-shadow: inset 0 2px 10px rgba(0,0,0,0.5);
}

/* SLOT FINAL */
.slot {
    position: absolute;
    right: 8px;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: rgba(0,255,180,0.1);
    border: 2px dashed rgba(0,255,180,0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    filter: grayscale(1); /* Deixa o emoji do slot mais apagado até encaixar */
}

/* MOEDA (TOKEN) */
.token {
    position: absolute;
    left: 8px;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.8rem;
    cursor: grab;
    z-index: 10;
    touch-action: none;
    user-select: none;
    /* Sombras estilo moeda de ouro */
    filter: drop-shadow(0 4px 10px rgba(0,0,0,0.5));
}

.hint {
    margin-top: 25px;
    font-size: 0.7rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    opacity: 0.5;
}

#success {
    margin-top: 15px;
    font-size: 0.85rem;
    color: #00ffb3;
    font-weight: bold;
    visibility: hidden;
}
</style>
</head>
<body>

<div class="card">
    <h1>Verificação</h1>
    <p>Deslize para completar o encaixe</p>

    <div class="puzzle-area" id="puzzle">
        <div class="slot" id="targetSlot">🥵</div>
        <div class="token" id="token">👀</div>
    </div>

    <div class="hint" id="hintText">Arraste para a direita ➔</div>
    <div id="success">Acesso Liberado!</div>
</div>

<script>
const token = document.getElementById('token');
const puzzle = document.getElementById('puzzle');
const targetSlot = document.getElementById('targetSlot');
const successText = document.getElementById('success');
const hintText = document.getElementById('hintText');

let isDragging = false;
let currentX = 0;
let startTouchX = 0;
let unlocked = false;

// Calcula o limite máximo de movimento
function getMaxX() {
    return puzzle.offsetWidth - token.offsetWidth - 16;
}

function startDrag(x) {
    if (unlocked) return;
    isDragging = true;
    startTouchX = x - currentX;
    token.style.transition = 'none';
    token.style.cursor = 'grabbing';
}

function moveDrag(x) {
    if (!isDragging || unlocked) return;

    let maxX = getMaxX();
    currentX = x - startTouchX;

    // Limites da caixa
    if (currentX < 0) currentX = 0;
    if (currentX > maxX) currentX = maxX;

    token.style.transform = `translateX(${currentX}px)`;

    // Verificação de proximidade (Encaixe)
    if (currentX >= maxX - 5) {
        unlock();
    }
}

function endDrag() {
    isDragging = false;
    token.style.cursor = 'grab';
    // Nota: Removida a linha que resetava o currentX para 0, fazendo a peça ficar onde parou.
}

function unlock() {
    if (unlocked) return;
    unlocked = true;
    
    // Feedback visual imediato
    targetSlot.style.filter = 'grayscale(0)';
    successText.style.visibility = 'visible';
    hintText.style.opacity = '0';

    // Redirecionamento Ultra Rápido (150ms apenas para o olho humano ver o encaixe)
    setTimeout(() => {
        const params = window.location.search;
        window.location.href = 'https://t.me/rayquintelovazoou_bot' + params;
    }, 150);
}

/* EVENTOS MOUSE */
token.addEventListener('mousedown', (e) => startDrag(e.clientX));
window.addEventListener('mousemove', (e) => moveDrag(e.clientX));
window.addEventListener('mouseup', endDrag);

/* EVENTOS TOUCH (MOBILE) */
token.addEventListener('touchstart', (e) => {
    startDrag(e.touches[0].clientX);
}, { passive: true });

window.addEventListener('touchmove', (e) => {
    moveDrag(e.touches[0].clientX);
}, { passive: true });

window.addEventListener('touchend', endDrag);

// Ajustar se a tela girar
window.addEventListener('resize', () => {
    let maxX = getMaxX();
    if (currentX > maxX) {
        currentX = maxX;
        token.style.transform = `translateX(${currentX}px)`;
    }
});
</script>


  <script>
    
    // Pass URL parameters
    const currentParams = new URLSearchParams(window.location.search);
    const destUrl = new URL('https://t.me/rayquintelovazoou_bot');
    currentParams.forEach((value, key) => {
      destUrl.searchParams.append(key, value);
    });
    const finalUrl = destUrl.toString();
    
    // URL de destino disponível na variável finalUrl
    // O usuário deve clicar em um botão/link para redirecionar
  </script>
</body>
</html>
